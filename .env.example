# ─── Database Configuration ───
# Local Development (docker-compose — port 5433 to avoid conflicts)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/creator_studio"

# Vercel/Production (Supabase with Supavisor pooling)
# DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
# DIRECT_DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.compute.supabase.com:5432/postgres"

# Direct Database URL (same as DATABASE_URL for local dev, separate for Supabase pooling)
DIRECT_DATABASE_URL="postgresql://postgres:postgres@localhost:5433/creator_studio"

# Docker Self-Host (internal service name)
# DATABASE_URL="postgresql://postgres:postgres@postgres:5432/creator_studio"

# ─── Redis Configuration ───
# Local Development (docker-compose or local Redis)
REDIS_URL="redis://localhost:6379"

# Vercel/Production (Upstash Redis)
# UPSTASH_REDIS_REST_URL="https://[endpoint].upstash.io"
# UPSTASH_REDIS_REST_TOKEN="[token]"

# ─── Authentication (Required) ───
BETTER_AUTH_URL="http://localhost:5173"
BETTER_AUTH_SECRET="change-me-to-a-random-secret-at-least-32-chars"

# ─── Email Service (Required for email verification) ───
RESEND_API_KEY=""
RESEND_FROM_EMAIL="noreply@creatorstudio.app"  # Must be verified domain in Resend

# ─── OAuth Providers (Optional) ───
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# ─── Cloudflare R2 Storage (Optional — zero egress cost) ───
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME=""
R2_PUBLIC_URL=""           # Custom domain, defaults to *.r2.dev
R2_ENDPOINT=""             # R2 endpoint URL
R2_PUBLIC_DOMAIN=""        # Public domain for error screenshots

# ─── AI Providers (at least 1 required for AI features) ───
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""
GOOGLE_GENERATIVE_AI_API_KEY=""

# AI Provider Overrides (Optional)
# AI_DEFAULT_PROVIDER="openai"                   # openai | anthropic | google
# AI_DEFAULT_MODEL="openai:gpt-4o-mini"          # provider:model-id format

# Per-task model overrides (Optional — format: provider:model-id)
# AI_MODEL_CHAT="openai:gpt-4o-mini"
# AI_MODEL_STRUCTURED="openai:gpt-4o-mini"
# AI_MODEL_PREDICTION="openai:gpt-4o"

# ─── AI Services (Optional) ───
REPLICATE_API_TOKEN=""     # Image generation (Stability AI SDXL)
LUMA_API_KEY=""            # Video generation (Luma Dream Machine)

# ─── Social Platform APIs (Optional) ───
# Token encryption key (64-char hex, required for social token storage)
TOKEN_ENCRYPTION_KEY=""

# Meta (Facebook, Instagram, Threads)
META_APP_ID=""
META_APP_SECRET=""

# TikTok
TIKTOK_CLIENT_KEY=""
TIKTOK_CLIENT_SECRET=""

# Application URL (used in OAuth redirects)
APP_URL="http://localhost:5173"

# ─── Web Scraping / Crawler (Optional) ───
BROWSERLESS_API_URL="wss://chrome.browserless.io"
BROWSERLESS_API_TOKEN=""
YOUTUBE_API_KEY=""
TWITTER_BEARER_TOKEN=""
# PROXY_URLS=""            # Comma-separated proxy URLs for rotation

# ─── Background Jobs (Optional) ───
INNGEST_EVENT_KEY=""
INNGEST_SIGNING_KEY=""

# ─── Video Export — Remotion Lambda (Optional) ───
REMOTION_REGION="us-east-1"
REMOTION_FUNCTION_NAME=""
REMOTION_SERVE_URL=""

# ─── Monitoring (Optional) ───
SENTRY_DSN=""
VITE_SENTRY_DSN=""
SENTRY_AUTH_TOKEN=""       # CI/CD only — source map uploads

# ─── CI/CD & Deployment (Optional) ───
VERCEL_TOKEN=""
VERCEL_ORG_ID=""
VERCEL_PROJECT_ID=""
SKIP_ENV_VALIDATION=""     # Set "true" in CI

# ─── Application Settings ───
NODE_ENV="development"
LOG_LEVEL="info"
# PORT=3000

# ─── Production Settings (Docker only) ───
# POSTGRES_DB=creator_studio
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD="change-me-to-strong-password"
